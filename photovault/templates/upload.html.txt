<!--
PhotoVault - Professional Photo Management Platform
Copyright (c) 2025 Calmic Sdn Bhd. All rights reserved.
-->

{% extends "base.html" %}

{% block title %}Upload Photos{% endblock %}

{% block extra_css %}
<style>
.upload-area {
    border: 2px dashed #dee2e6;
    border-radius: 4px;
    padding: 2rem;
    text-align: center;
    background-color: #f8f9fa;
    transition: all 0.3s ease;
    cursor: pointer;
}

.upload-area:hover {
    border-color: var(--primary-color);
    background-color: #e9ecef;
}

.upload-area.dragover {
    border-color: #007bff;
    background-color: #e7f3ff;
    transform: scale(1.02);
}

.upload-area input[type="file"] {
    position: absolute;
    opacity: 0;
    width: 100%;
    height: 100%;
    cursor: pointer;
}

.camera-preview {
    background-color: #f8f9fa;
    border: 1px solid #dee2e6;
    border-radius: 4px;
    min-height: 200px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.camera-preview video {
    max-width: 100%;
    max-height: 300px;
    border-radius: 4px;
}

.camera-placeholder {
    text-align: center;
    color: #6c757d;
}

#captureBtn {
    background-color: #dc3545;
    border-color: #dc3545;
    transition: all 0.3s ease;
}

#captureBtn:hover {
    background-color: #c82333;
    transform: scale(1.05);
}

.btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}

@media (max-width: 768px) {
    .upload-area {
        padding: 1.5rem 1rem;
    }
    
    .camera-preview {
        min-height: 150px;
    }
}
</style>
{% endblock %}

{% block content %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
            <!-- Main Upload Card -->
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">
                        <i class="bi bi-cloud-upload"></i> Upload Photos
                    </h4>
                    <small class="text-muted">Choose files or capture photos to upload</small>
                </div>
                <div class="card-body">
                    <!-- Upload Form -->
                    <form id="uploadForm" enctype="multipart/form-data">
                        <!-- File Upload Area -->
                        <div class="upload-area mb-4" id="uploadArea">
                            <div id="uploadStatus">
                                <i class="bi bi-cloud-upload" style="font-size: 3rem; color: #6c757d;"></i>
                                <h5 class="mt-3">Choose Photos to Upload</h5>
                                <p class="text-muted">Drag and drop files here, or click to select</p>
                            </div>
                            <input type="file" 
                                   id="file" 
                                   name="files[]" 
                                   accept="image/*" 
                                   multiple 
                                   required>
                        </div>
                        
                        <div class="row">
                            <div class="col-12">
                                <small class="form-text text-muted">
                                    <i class="bi bi-info-circle"></i>
                                    Supported formats: PNG, JPG, JPEG, GIF, WEBP (Max 16MB per file)
                                </small>
                            </div>
                        </div>
                        
                        <!-- Upload Button -->
                        <div class="d-grid mt-3">
                            <button type="submit" class="btn btn-primary" id="uploadBtn" disabled>
                                <i class="bi bi-upload"></i> Upload Photos
                            </button>
                        </div>
                    </form>
                </div>
            </div>
            
            <!-- Camera Card -->
            <div class="card mt-4">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="bi bi-camera"></i> Camera Capture
                    </h5>
                    <small class="text-muted">Take photos directly with your camera</small>
                </div>
                <div class="card-body">
                    <!-- Camera Controls -->
                    <div class="row mb-3">
                        <div class="col-md-8">
                            <label for="cameraSelect" class="form-label">Select Camera:</label>
                            <select class="form-select" id="cameraSelect">
                                <option value="">Loading cameras...</option>
                            </select>
                        </div>
                        <div class="col-md-4 d-flex align-items-end">
                            <button type="button" 
                                    class="btn btn-success w-100" 
                                    id="startCameraBtn" 
                                    disabled>
                                <i class="bi bi-camera-video"></i> Start Camera
                            </button>
                        </div>
                    </div>
                    
                    <!-- Camera Preview -->
                    <div class="camera-preview mb-3" id="cameraPreview">
                        <div class="camera-placeholder">
                            <i class="bi bi-camera" style="font-size: 2rem;"></i>
                            <p class="mt-2 mb-0">Camera preview will appear here</p>
                            <small>Select a camera and click "Start Camera"</small>
                        </div>
                        <video id="cameraVideo" 
                               autoplay 
                               muted 
                               playsinline 
                               style="display: none;"></video>
                    </div>
                    
                    <!-- Capture Button -->
                    <div class="text-center">
                        <button type="button" 
                                class="btn btn-danger" 
                                id="captureBtn" 
                                style="display: none;">
                            <i class="bi bi-camera-fill"></i> Capture Photo
                        </button>
                    </div>
                    
                    <!-- Hidden canvas for photo capture -->
                    <canvas id="captureCanvas" style="display: none;"></canvas>
                </div>
            </div>
            
            <!-- Navigation -->
            <div class="card mt-4">
                <div class="card-footer">
                    <div class="d-flex justify-content-between">
                        <a href="{{ url_for('main.dashboard') }}" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Back to Dashboard
                        </a>
                        <a href="{{ url_for('main.originals') }}" class="btn btn-outline-primary">
                            <i class="bi bi-images"></i> View Gallery
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Upload Progress/Messages -->
    <div id="uploadProgress" class="alert alert-info mt-3" style="display: none;" role="alert">
        <!-- Dynamic content will be inserted here -->
    </div>
</div>
{% endblock %}

{% block extra_js %}
<!-- Load the fixed upload JavaScript -->
<script src="{{ url_for('static', filename='js/upload.js') }}"></script>
{% endblock %}